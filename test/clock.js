var chai = require('chai');
var expect = require('chai').expect;
var clock = require('../app/app.js');

describe('Clock', function() {
  describe('Constructor', function() {
    it('should have a seconds property', function() {

    });
  });
});
